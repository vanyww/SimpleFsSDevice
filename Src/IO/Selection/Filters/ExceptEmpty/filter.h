#pragma once

#include "../selection_filter_interface.h"

SELECTION_FILTER_CONTEXT_FORWARD_DECLARATION(ExceptEmpty);
SELECTION_FILTER_PARAMETERS_FORWARD_DECLARATION(ExceptEmpty);

struct SELECTION_FILTER_CONTEXT(ExceptEmpty)
{

};

struct SELECTION_FILTER_PARAMETERS(ExceptEmpty)
{

};

const extern SelectionFilterInterface SELECTION_FILTER_INTERFACE(ExceptEmpty);
